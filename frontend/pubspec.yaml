name: bolekpad_frontend
description: "A new Flutter project for Bolek Docs."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  go_router: ^13.1.0
  freezed_annotation: ^2.4.1
  drift: ^2.14.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  build_runner: ^2.4.8
  freezed: ^2.4.7
  drift_dev: ^2.14.1

flutter:
  uses-material-design: true
