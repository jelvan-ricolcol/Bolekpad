{
  "name": "bolekpad-monorepo",
  "private": true,
  "workspaces": [
    "legacy",
    "backend"
  ],
  "scripts": {
    "build": "node -e \"require('fs').rmSync('public', {recursive: true, force: true})\" && npm run build --workspace=legacy && node -e \"require('fs').cpSync('legacy/dist', 'public', {recursive: true})\""
  }
}
