{
  "name": "bolekpad-monorepo",
  "private": true,
  "workspaces": [
    "legacy",
    "backend"
  ],
  "scripts": {
    "build": "rm -rf public && npm run build --workspace=legacy && cp -r legacy/dist public"
  }
}
