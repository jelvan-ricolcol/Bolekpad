{
  "name": "bolekpad-monorepo",
  "private": true,
  "workspaces": [
    "legacy",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace=legacy && rm -rf public && cp -r legacy/dist public"
  }
}
